[project]
name = "bus-reports-exporter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "notebook>=7.3.2",
    "pandas>=2.2.3",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.4",
]
